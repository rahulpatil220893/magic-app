import React from "react";

function NotebookImageAlertPopup({ noteBookData }) {
  return (
    <div className="vl-notebook-alert-content">{noteBookData.imageAlert}</div>
  );
}

export default NotebookImageAlertPopup;
