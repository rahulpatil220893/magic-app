import React from "react";

function SavePopup({ messagesData }) {
  return (
    <div className="vl-popup-content-container">
      <p>{messagesData.save}</p>
    </div>
  );
}

export default SavePopup;
